# ==========================================
# AI-TINERARY MASTER CONFIGURATION
# Edit this file directly to control the Python pipeline.
# It uses dotenv format, so exported variables will be read by
# the main script. No need to copy it anywhere – changes take
# effect immediately the next time you run `python AI-tinerary`.
#
# NOTE: the setup script will create this file if it is missing,
# populating it with defaults you can tweak.
# ==========================================

# Home base for mileage estimation (Required)
HOME_BASE_ADDRESS=""

# OpenRouteService settings (optional)
# You can provide a public API key or point at a local container.  The
# container defaults to http://localhost:8080/ors/v2 when run via Docker.
# Get a free key at: https://openrouteservice.org/
ORS_API_KEY=""
ORS_BASE_URL=""

# Ollama Settings (Local AI)
OLLAMA_URL="http://localhost:11434/api/generate"
OLLAMA_MODEL="ministral-3:3b"

# Processing Settings
# Number of files to process simultaneously
MAX_THREADS=4

# -----------------------------------------
# Discord bot (used by AI-tinerary-CALBOT.py)
# Create a Discord application/bot and paste the token here so the
# helper script can connect.  This variable is not required for the
# core CSV extraction workflow, so you can leave it empty if you
# don't plan to run the bot.
DISCORD_BOT_TOKEN=""

# -----------------------------------------
# Google Calendar integration
# Provide the path to a service-account JSON file with calendar
# permissions, and set two calendar IDs (band/internal and public)
GOOGLE_SERVICE_ACCOUNT_FILE=""
BAND_CALENDAR_ID=""
PUBLIC_CALENDAR_ID=""
